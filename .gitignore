# Local agent/tool settings
.claude/

# Local Crystal cache
.crystal-cache/

# Temporary working artifacts
temp/

# Dependency directories (exclude only when not used as committed source-of-truth)
# vendor/

# Crystal build artifacts
/lib/
/.shards/
*.dwarf

# Libraries don't need dependency lock
# Dependencies will be locked in applications that use them
/shard.lock
